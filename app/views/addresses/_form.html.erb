<%= form_for([@address.user,@address]) do |f| %>
  <%= f.hidden_field :user_id %>
  

  <div class="field">
    <%= f.label :desc %>
    <%= f.text_field :desc %>
  </div>

  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
